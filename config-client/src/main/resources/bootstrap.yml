server:
  port: 5589

spring:
  application:
    name: configspace
  cloud:
    config:
      label: master
      # dev 开发环境配置文件 |  test 测试环境  |  pro 正式环境
      profile: dev
      uri: http://127.0.0.1:5588/
